# QMK Firmware for ZSA Moonlander MK1

## Setup

- clone repo with submoduels
- start `default` nix devShell

## Usage

Compile firmware:
```sh
just build
```

Flash keyboard:
```sh
sudo just flash
```
